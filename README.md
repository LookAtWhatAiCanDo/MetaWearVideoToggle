# MetaWearVideoToggle

Initial code generated by Claude and Codex with the following prompt:
```
You are an expert Android + WearOS engineer.

Generate a **complete, buildable Android Studio project** (Kotlin, modern APIs) consisting of:

* 1 Android phone app (primary app)
* 1 WearOS companion app
* Communication between them
* Integration with the **Meta Wearables Device Access Toolkit Android SDK**

---

# 🎯 Goal

Press a **"Video" button on the WearOS watch**:

* First press → start video recording using Meta AI Glasses camera
* Second press → stop recording
* Toggle behavior must be reliable and stateful

---

# 🧩 Functional Requirements

## WearOS App

* Single screen UI
* Large central button labeled:

  * "Start Video" (idle)
  * "Stop Video" (recording)
* Uses:

  * `MessageClient` (preferred) or `DataClient`
* Sends commands to phone:

  * `"VIDEO_START"`
  * `"VIDEO_STOP"`

---

## Phone App

### Core responsibilities:

* Maintain connection to Meta glasses via **Device Access Toolkit**
* Own recording session lifecycle
* Receive commands from watch
* Start/stop video capture

---

# 🔌 Meta Wearables SDK Integration

Assume the SDK provides (adapt if needed, but stay realistic):

* Device/session manager
* Camera/video capture API
* Audio/video stream callbacks

Implement:

* Initialization of SDK
* Device discovery / connection
* Camera stream start/stop
* Video recording pipeline (file-based, e.g., MP4)

---

# 🎥 Video Behavior

* On `"VIDEO_START"`:

  * Ensure device connected
  * Start camera stream
  * Begin recording to file
  * Save file locally (e.g., app-specific storage)

* On `"VIDEO_STOP"`:

  * Stop recording cleanly
  * Finalize file
  * Log file path

* Handle edge cases:

  * Already recording
  * Device disconnected
  * SDK errors

---

# 🔁 State Management

* Single source of truth on phone:
````
  ```kotlin
  enum class RecordingState {
      IDLE,
      RECORDING
  }
  ```
````
* Watch UI must reflect actual state:

  * Sync state from phone → watch
  * Use `DataClient` or message response

---

# 📡 Communication Layer

Implement:

* Wear → Phone:

  * MessageClient sendMessage

* Phone → Wear:

  * Acknowledgment + state sync

* Include:

  * Node discovery
  * Connection handling
  * Retry logic

---

# 🧱 Architecture

Use:

* Kotlin
* Coroutines + Flow
* Clean separation:

  * `WatchMessageHandler`
  * `RecordingController`
  * `MetaDeviceManager`

---

# 📂 Project Structure

Provide full structure:

* `/mobile`
* `/wear`

Include:

* Gradle files (KTS preferred)
* Manifest files
* Permissions:

  * Bluetooth
  * Nearby devices (Android 12+)
  * Audio/video if needed

---

# ⚠️ Constraints

* Do NOT stub everything — provide real, implementable logic
* If SDK APIs are unknown:

  * define realistic interfaces and clearly mark them
* Avoid deprecated APIs
* Must compile with recent Android SDK

---

# ✅ Output Format

Provide:

1. Project directory tree
2. Key files in full:

   * MainActivity (phone + watch)
   * Wear UI
   * Message handling classes
   * Recording controller
3. Gradle configs
4. Brief setup instructions

---

# 🧠 Bonus (include if possible)

* Debounce rapid presses
* Foreground service for recording
* Notification showing recording state
* Basic logging

---

Be concise but complete. No placeholders like “implement here.”
Produce real Kotlin code.
```
